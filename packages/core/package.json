{
  "name": "winston",
  "version": "0.1.0",
  "description": "AI-native intelligence layer for cannabis retailers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "NODE_ENV=test node --experimental-vm-modules node_modules/.bin/jest",
    "install:dev": "NODE_ENV=development npm install",
    "lint": "eslint src tests --ext .ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "express": "^5.2.1",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.37",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.9.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
